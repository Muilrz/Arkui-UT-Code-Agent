agent:
  system_template: |
    You are a helpful assistant that can interact with a computer.
  instance_template: |
    Please solve this issue: {{task}}

    You can execute {{shell_dialect}} commands and edit files to implement the necessary changes.
    The command tool is named `bash` for protocol compatibility, but commands are executed unchanged by
    {{shell_name}} on {{os_name}}. Use only {{shell_dialect}} syntax; do not write commands for another shell.

    ## Recommended Workflow

    This workflow should be done step-by-step so that you can iterate on your changes and any possible problems.

    1. Analyze the codebase by finding and reading relevant files
    2. Create a script to reproduce the issue
    3. Edit the source code to resolve the issue
    4. Verify your fix works by running your script again
    5. Test edge cases to ensure your fix is robust
    6. Submit your changes and finish your work by issuing the following command: `echo COMPLETE_TASK_AND_SUBMIT_FINAL_OUTPUT`.
       Do not combine it with any other command. <important>After this command, you cannot continue working on this task.</important>

    ## Command Execution Rules

    You are operating in an environment where

    1. You issue at least one command
    2. The system executes the command(s) in a new {{shell_name}} process
    3. You see the result(s)
    4. You write your next command(s)

    Each response should include:

    1. **Reasoning text** where you explain your analysis and plan
    2. At least one tool call with your command

    **CRITICAL REQUIREMENTS:**

    - Your response SHOULD include reasoning text explaining what you're doing
    - Your response MUST include AT LEAST ONE command tool call
    - Directory or environment variable changes are not persistent. Every action is executed in a new shell process.
    - Use {{shell_dialect}} syntax for temporary environment or directory changes.
    - Submit your changes and finish your work by issuing the following command: `echo COMPLETE_TASK_AND_SUBMIT_FINAL_OUTPUT`.
      Do not combine it with any other command. <important>After this command, you cannot continue working on this task.</important>

    Example of a CORRECT response:
    <example_response>
    I need to understand the structure of the repository first. Let me check what files are in the current directory to get a better understanding of the codebase.

    [Calls the command tool with a command written in {{shell_dialect}} syntax]
    </example_response>

    <system_information>
    {{system}} {{release}} {{version}} {{machine}}
    Operating system: {{os_name}}
    Shell backend: {{shell_name}}
    Shell dialect: {{shell_dialect}}
    </system_information>

    ## Shell examples

    {% if shell_dialect == "powershell" -%}
    Use PowerShell commands such as `Get-ChildItem`, `Get-Content`, `Set-Content`, and `Select-String`.
    Environment variables use `$env:NAME`; chaining and redirection must follow PowerShell syntax.
    {%- else -%}
    Use {{shell_dialect}} commands such as `ls`, `cat`, `printf`, `sed`, and `grep`.
    Environment variables use `$NAME`; chaining and redirection must follow POSIX shell syntax.
    {%- endif %}
  step_limit: 0
  cost_limit: 3.
  mode: confirm
environment:
  shell_backend: auto
  env:
    PAGER: cat
    MANPAGER: cat
    LESS: -R
    PIP_PROGRESS_BAR: 'off'
    TQDM_DISABLE: '1'
model:
  observation_template: |
    {%- if output.output | length < 10000 -%}
    {
      "returncode": {{ output.returncode }},
      "output": {{ output.output | tojson }}
      {%- if output.exception_info %}, "exception_info": {{ output.exception_info | tojson }}{% endif %}
    }
    {%- else -%}
    {
      "returncode": {{ output.returncode }},
      "output_head": {{ output.output[:5000] | tojson }},
      "output_tail": {{ output.output[-5000:] | tojson }},
      "elided_chars": {{ output.output | length - 10000 }},
      "warning": "Output too long."
      {%- if output.exception_info %}, "exception_info": {{ output.exception_info | tojson }}{% endif %}
    }
    {%- endif -%}
  format_error_template: |
    {% if finish_reason is defined and (finish_reason == "length" or (finish_reason == "tool_calls" and not has_tool_calls)) -%}
    Your previous response reached the output token limit (finish_reason={{ finish_reason }}) before you produced a tool call, so it was cut off. Respond more concisely and finish with exactly one command tool call. If you need to think more, do so briefly.
    {%- else -%}
    Tool call error:

    <error>
    {{error}}
    </error>

    Here is general guidance on how to submit correct toolcalls:

    Every response needs to use the `bash` command tool at least once. The tool name is protocol compatibility only;
    its command must use the shell dialect described in the task context.

    Call the bash tool with your command as the argument:
    - Tool: bash
    - Arguments: {"command": "your_command_here"}

    If you want to end the task, please issue the following command: `echo COMPLETE_TASK_AND_SUBMIT_FINAL_OUTPUT`
    without any other command.
    {%- endif %}
  model_kwargs:
    drop_params: true
